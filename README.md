#class Euclidean Algorithm GCD:
def_init_(self):
def GCD(self, A,B):
    self.A=A
    self.B=B
    if A ==0 and B == 0:
        print("error A and B can't be 0 at same time")
    if A < 0 or B < 0:
        print("error A and B can't be negative")
    while b !=0 and B > A:
        remainder = A%B
        A=B 
        B=remainder
    return A
def is_useful_input(self, num):
    num= int(num)
    return num >0
    except("error A and B can't be 0 at same time","error A and B can't be negative")
def get_input():
    calculator = Euclidean Algorithm
    while True:
        print("plz enter 2 positive integer")

"""get the first input"""        
        input 1 =input("plz enter first positive integer")
        
"""get the second input"""
        input 2 =input("plz enter second positive integer")
        
        num1 = int(input1)
        num2 = int(input2)
        
        return num1, num2
    
        
    
        
 
